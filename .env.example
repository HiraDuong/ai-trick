# Copy to apps/backend/.env and apps/frontend/.env.local as needed.

# --- Backend (apps/backend/.env) ---
NODE_ENV=development
PORT=4000
# Matches infra/docker-compose.yml default credentials
DATABASE_URL="postgresql://ikp:ikp_dev_secret@localhost:5432/ikp?schema=public"

# --- Frontend (apps/frontend/.env.local) ---
# Optional: API base URL for future BFF / proxy usage
# NEXT_PUBLIC_API_URL=http://localhost:4000
